unzip pokemon-db.zip && cd pokemon-db

# 1. Create DB and apply schema
psql -U postgres -c "CREATE DATABASE starter_db;"
psql -U postgres -d starter_db -f schema.sql

# 2. Install pg driver and seed
npm install
npm run seed

PGPASSWORD=projectpokedex npm run seed